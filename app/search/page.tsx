// import Image from "next/image";

import { Sidebar } from "@/components/ui/sidebar"

export default function Search() {
  return (
    <div>
      <div className="flex">
        <Sidebar />
        <p>Search page</p>
        <p>Test</p>
      </div>
    </div>
  );
}

// A search page is a part of a website or app where you can type in words to find
// specific information. It usually has a search bar where you enter keywords, and when
// you press Enter or click a button, the website looks through its data to show you the
// best matches.